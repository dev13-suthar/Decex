/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['images.pexels.com','lh3.googleusercontent.com','encrypted-tbn0.gstatic.com','upload.wikimedia.org']
    }
};

export default nextConfig;
